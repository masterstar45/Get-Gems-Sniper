[build]
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python artifacts/getgems-bot/bot/bot.py"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "always"
